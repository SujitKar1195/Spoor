<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
	integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<style>
	body {
		background-color: #212121;
	}

	hr {
		color: red;
	}

	.navbar {

		font-weight: 400;
	}

	.nav-link {
		color: black;
	}

	.dropdown-toggle {
		font-weight: 600;
	}


	.dropdown-item {
		background: rgb(140, 140, 140);
	}

	.navbar-toggler-icon {
		background-color: white;
	}

	.todo-item {
		width: 12rem;
		border-radius: 15px;
		padding: 1.5rem;
		background: rgb(79, 78, 78);
		align-items: flex-end;
		transition: 0.4s ease-out;
		box-shadow: 0px 7px 10px rgba(rgb(0, 0, 0), 0.5);
		overflow: hidden;

	}

	.todo-item:hover {
		transform: translateY(0.3rem);
	}

	.todo-title {
		text-align: center;
		font-weight: bold;
		font-size: 20px;
		color: rgb(246, 221, 244);
	}

	.todo-description {
		color: white;
		margin-top: 10px;
		margin-bottom: 10px;
	}

	.todo-completed {
		color: white;
		margin-bottom: 10px;
	}

	.delete-button {
		font-weight: bold;
		font-size: 20px;
		background-color: red;
		color: white;
		border: none;
		padding: 5px 10px;
		cursor: pointer;
		border-radius: 0.4rem;
	}

	.delete-button:hover {
		background-color: #672375;
	}

	@keyframes firecracker {
		0% {
			transform: translateY(0) rotate(0);
			opacity: 1;
		}

		100% {
			transform: translateY(-100px) rotate(360deg);
			opacity: 0;
		}
	}

	.firecracker-animation {
		animation: firecracker 1s ease-out;
	}
</style>